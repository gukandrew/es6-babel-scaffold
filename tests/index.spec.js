import Main from '../src/index.js'

/* eslint-disable no-multi-str */
describe('index.js', () => {
  describe('test', () => {
    // it('*', async () => { })
  })
})

/* eslint-enable no-multi-str */
